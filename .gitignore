# Local working notes (assignment scratch, decision drafts) — never shipped
.notes/

# Env & secrets
.env
.env.*
!.env.example

# Node / build
node_modules/
.next/
dist/
.vercel/

# Generated artifacts and runtime data (regenerated by `npm run demo`)
build/
data/cards.jsonl
data/visits.jsonl

# Test output
test-results/
playwright-report/
coverage/

# Downloaded fonts (code falls back to system fonts)
assets/fonts/

# Claude Code local state
.claude/settings.local.json

# OS
.DS_Store

# SDD scratch (progress ledger)
.superpowers/
